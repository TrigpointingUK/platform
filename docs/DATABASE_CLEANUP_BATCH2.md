# Database Cleanup Summary (Legacy Schema)

This repository contains Alembic migrations that removed a number of legacy tables and columns from the historic schema.

- The authoritative source of what changed is the relevant Alembic revision under `alembic/versions/`.
- This document intentionally omits specific legacy table names to avoid reintroducing stale references.
