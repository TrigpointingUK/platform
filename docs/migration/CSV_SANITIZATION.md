# CSV Sanitisation

This document covers CSV sanitisation performed during the MySQL→PostgreSQL migration work.

## Notes

To avoid reintroducing stale references to removed legacy tables, this document intentionally omits specific legacy table names.
