anytree==2.13.0
bd_warehouse==0.3.0
build123d==0.11.1
cadquery-ocp-novtk==8.0.1.0.0
cadquery-ocp-proxy==7.9.3.1.1
ezdxf==1.4.4
networkx==3.6.1
numpy==2.5.3
ocpsvg==0.6.0
scipy==1.18.1
segno==1.6.6
svgpathtools==1.7.2
trianglesolver==1.2
trimesh==5.0.0

# Needed by the flush-bracket model as well as by fbfit. The casting draft on
# the raised lettering is computed as a distance transform on a raster, because
# OCCT's own draft and 2-D offset operations fail on glyph outlines (S and B
# fail outright, M once squeezed to its measured box). See
# models/flush_bracket/relief.py.
opencv-python-headless==5.0.0.93
